{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Gem",
  "type": "object",
  "properties": {
    "url": { "type": "string", "format": "uri" },
    "description": { "type": "string" },
    "author": { "type": "string", "description": "Optional. Can be omitted or left blank." },
    "date": { "type": "string", "format": "date", "description": "Set by the server on submission." }
  },
  "required": ["url", "description"],
  "additionalProperties": false
}