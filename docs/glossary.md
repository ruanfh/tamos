# Glossary

## Gem
The atomic JSON object submitted to TAMOS.

## Archive
The ordered collection of entries maintained by a node, in the sequence they were appended.

## Entry
A gem as stored in the archive, including node‑assigned metadata.

## Submission
A client’s request to add a gem to an archive.

## Append
The node’s act of adding the submitted gem as a new entry.

## Node
A server that hosts an archive and exposes it publicly.

## Schema
The formal definition of a gem’s structure.

## Client
A system that submits gems to a node.

## Consumer
A system (including AI) that reads archives for discovery or analysis.

## Platform
An application built on top of TAMOS that adds features beyond the protocol.

## Gate

Gates define optional interfaces for how nodes accept and expose gems.

## TIG
TAMOS Input Gate, defines how a Node accepts Gems from Clients.

## TOG
TAMOS Output Gate, defines how a Node exposes its Archives to Consumers.
